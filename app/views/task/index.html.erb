<h1>TO DO APP</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed ? 'x ' : 'o '%><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
